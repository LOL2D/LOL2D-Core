class AhriAttack extends AbilityCore {
    constructor(config = {}) {
        super(config);
    }

    // override
    showIndicator() {}

    // override
    castSpell(destination) {}

    // override
    onStarted() {}

    // override
    onFinished() {}
}
