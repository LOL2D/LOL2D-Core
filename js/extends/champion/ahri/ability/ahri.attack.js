class AhriAttack extends AbilityCore {
    constructor(config = {}) {
        super(config);
    }

    // override
    preview() {}

    // override
    castSpell(destination) {}

    // override
    onStarted() {}

    // override
    onFinished() {}
}
